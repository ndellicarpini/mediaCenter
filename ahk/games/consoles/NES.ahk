MsgBox("hello")

; console should take emuObj, rom, and controller to setup proper launch configs
; emuobj should hold all of the settings of the emulator & controller options
; rom & currentController passed through